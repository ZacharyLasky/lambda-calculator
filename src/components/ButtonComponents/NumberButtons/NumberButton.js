import React from "react";

const NumberButton = (props) => {
  return (
    
      <button className="num-class">
        {/* Display a button element rendering the data being passed down from the parent container on props */
        props}
      </button>
    
    );
};

export default NumberButton;